"""Test suite for WLASL300 Sign Language Recognition.

Run all tests::

    uv run pytest

Fast tests only (exclude slow GPU / integration tests)::

    uv run pytest -m "not slow and not integration"

With coverage report::

    uv run pytest --cov
"""
